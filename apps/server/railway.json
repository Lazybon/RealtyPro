{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd ../.. && npm ci --include=dev && npm run build:server",
    "watchPatterns": [
      "apps/server/**",
      "packages/database/**",
      "package.json",
      "package-lock.json"
    ]
  },
  "deploy": {
    "startCommand": "cd ../.. && npm run start:server",
    "restartPolicyType": "ON_FAILURE"
  }
}
